<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>All Projects</title>

    <!-- ✅ Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <div class="container mt-4">
        <h2 class="text-center">All Projects</h2>

        <div class="table-responsive">  <!-- Makes table responsive -->
            <table class="table table-bordered table-hover text-center">
                <thead class="table-dark">
                    <tr>
                        <th>#</th>
                        <th>Project Name</th>
                        <th>Description</th>
                        <th>Client</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                        <th>Added By</th>
                    </tr>
                </thead>
                <tbody>

                    {{project.name}}
                    {% for project in projects %}
                    
                    <tr>
                        <td>{{ forloop.counter }}</td>
                        <td>{{ project.name }}</td>
                        <td>{{ project.description }}</td>
                        <td>{{ project.client }}</td>
                        <td>{{ project.start_date|date:"Y-m-d H:i" }}</td>
                        <td>
                            {% if project.end_date %}
                                {{ project.end_date|date:"Y-m-d H:i" }}
                            {% else %}
                                <span class="badge bg-success">Ongoing</span>
                            {% endif %}
                        </td>
                        <td>
                            {% if project.added_by %}
                                {{ project.added_by.username }}
                            {% else %}
                                <span class="text-muted">Unknown</span>
                            {% endif %}
                        </td>
                    </tr>

                    
                    {% empty %}
                    <tr>
                        <td colspan="7" class="text-center text-danger fw-bold">No projects available.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
